# دليل إعداد Firebase ونشر قاعدة بيانات الموردين

الموقع الآن جاهز للعمل مع Firebase (تخزين سحابي + تسجيل دخول آمن). ينقصه شيء واحد فقط: أن تربطه بمشروعك في Firebase. اتبع الخطوات بالترتيب.

---

## الخطوة ١ — إنشاء مشروع Firebase

1. افتح <https://console.firebase.google.com> وسجّل الدخول بحساب Google.
2. اضغط **Add project** واختر اسماً (مثلاً `vendor-database`).
3. يمكنك إيقاف Google Analytics — غير ضروري هنا. اضغط **Create project**.

## الخطوة ٢ — تفعيل تسجيل الدخول (Authentication)

1. من القائمة الجانبية: **Build ← Authentication ← Get started**.
2. افتح تبويب **Sign-in method**.
3. اختر **Email/Password**، فعّله (Enable)، واحفظ (Save).

## الخطوة ٣ — إنشاء قاعدة البيانات (Firestore)

1. من القائمة الجانبية: **Build ← Firestore Database ← Create database**.
2. اختر أقرب منطقة جغرافية لك (مثلاً `eur3` أو `me-central` إن توفّرت).
3. اختر **Start in production mode** ثم أكمل الإنشاء.
4. بعد الإنشاء افتح تبويب **Rules**، احذف المحتوى، والصق محتوى ملف `firestore.rules` المرفق، ثم اضغط **Publish**.

هذه القواعد تضمن أن الموردين لا يُقرأون ولا يُعدّلون إلا من مستخدم سجّل الدخول.

## الخطوة ٤ — الحصول على إعدادات المشروع ولصقها في الموقع

1. اضغط أيقونة الترس ⚙️ بجانب **Project Overview ← Project settings**.
2. انزل إلى **Your apps**، واضغط أيقونة الويب `</>`.
3. أعطِ التطبيق اسماً (مثلاً `vendor-web`) ثم **Register app**.
4. ستظهر لك كتلة `firebaseConfig` تشبه هذه:

   ```js
   const firebaseConfig = {
     apiKey: "AIza...",
     authDomain: "vendor-database.firebaseapp.com",
     projectId: "vendor-database",
     storageBucket: "vendor-database.appspot.com",
     messagingSenderId: "1234567890",
     appId: "1:1234...:web:abcd..."
   };
   ```

5. افتح ملف `index.html`، وابحث عن `firebaseConfig` قرب أعلى الصفحة، واستبدل قيم `PASTE_YOUR_...` بقيمك الحقيقية.

> **ملاحظة أمان مهمة:** وجود `apiKey` داخل الكود العام أمرٌ طبيعي وآمن في Firebase — فهو ليس كلمة سر. الحماية الفعلية تأتي من تسجيل الدخول ومن قواعد Firestore التي أعددتها في الخطوة ٣.

---

## الخطوة ٥ — رفع الكود على GitHub

1. أنشئ مستودعاً جديداً على GitHub.
2. ارفع الملفات: `index.html` و `firestore.rules` (وهذا الدليل إن أردت).

يمكنك الرفع عبر واجهة GitHub مباشرة (زر **Add file ← Upload files**) دون الحاجة لأوامر.

---

## الخطوة ٦ — النشر على Firebase Hosting

تحتاج إلى تثبيت Node.js أولاً من <https://nodejs.org> ثم في نافذة الأوامر (Terminal / CMD):

```bash
npm install -g firebase-tools
firebase login
firebase init hosting
```

عند `firebase init hosting`:
- اختر المشروع الذي أنشأته.
- عندما يسأل عن **public directory**، اكتب اسم المجلد الذي فيه `index.html` (مثلاً `.` إذا كان في نفس المكان، أو `public`).
- **Configure as a single-page app?** ← اكتب `No`.
- **Overwrite index.html?** ← اكتب `No` (مهم، حتى لا يُستبدل ملفك).

ثم انشر:

```bash
firebase deploy
```

سيعطيك رابطاً بالشكل `https://vendor-database.web.app` — هذا هو موقعك المباشر.

---

## أول استخدام

1. افتح الرابط، ستظهر شاشة الدخول. اضغط **Create one** وأنشئ حسابك ببريدك الإلكتروني.
2. بعد الدخول، أضف الموردين — تُحفظ سحابياً فوراً وتظهر على أي جهاز تفتح منه الموقع وتسجّل دخوله.

### لإضافة زملاء فريقك
- كل زميل ينشئ حسابه بنفس طريقة **Create one**، وسيرى نفس قائمة الموردين المشتركة.
- إذا أردت منع أي شخص خارجي من إنشاء حساب لاحقاً، يمكنك من **Authentication ← Settings ← User actions** إيقاف **Enable create (sign-up)** بعد أن ينشئ فريقك حساباتهم. حينها تُضاف الحسابات يدوياً من لوحة Firebase فقط.

---

## ملاحظة حول المزامنة
تُحمّل أحدث نسخة من الموردين عند فتح الموقع وتسجيل الدخول. إذا أضاف زميل مورداً بينما موقعك مفتوح، حدّث الصفحة (Refresh) لرؤية التغييرات.
